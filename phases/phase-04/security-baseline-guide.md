# Phase 4: Kubernetes Security Baseline

## Overview

Phase 4 implements two critical Kubernetes security features:

| Feature | Purpose |
|---------|---------|
| **NetworkPolicies** | Control pod-to-pod traffic (network segmentation) |
| **Pod Security Standards (PSS)** | Restrict pod capabilities and privileges |

---

# Part 1: NetworkPolicies

## What are NetworkPolicies?

By default, Kubernetes allows **all pods to communicate with all other pods** — this is a security risk. NetworkPolicies act as firewalls between pods.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     WITHOUT NetworkPolicies                              │
│                                                                          │
│                        ALL TRAFFIC ALLOWED                               │
│                                                                          │
│    ┌─────────┐         ┌─────────┐         ┌─────────┐                 │
│    │ Pod A   │◄───────►│ Pod B   │◄───────►│ Pod C   │                 │
│    │         │         │         │         │         │                 │
│    └─────────┘         └─────────┘         └─────────┘                 │
│         ▲                   ▲                   ▲                        │
│         │                   │                   │                        │
│         └───────────────────┴───────────────────┘                        │
│                     Any pod can talk to any pod                          │
│                           🚨 INSECURE                                    │
└─────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                      WITH NetworkPolicies                                │
│                                                                          │
│                     ONLY ALLOWED TRAFFIC                                 │
│                                                                          │
│    ┌─────────┐         ┌─────────┐         ┌─────────┐                 │
│    │ Pod A   │────────►│ Pod B   │    ✗    │ Pod C   │                 │
│    │         │         │         │◄───────►│         │                 │
│    └─────────┘         └─────────┘         └─────────┘                 │
│                             ▲                                            │
│                             │ Only allowed                               │
│                             ▼                                            │
│                        ┌─────────┐                                      │
│                        │ Pod D   │                                      │
│                        └─────────┘                                      │
│                          ✅ SECURE                                       │
└─────────────────────────────────────────────────────────────────────────┘
```

## NetworkPolicy Anatomy

```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: example-policy
  namespace: my-namespace        # Policy applies to this namespace
spec:
  podSelector:                   # Which pods this policy applies to
    matchLabels:
      app: my-app
  policyTypes:
    - Ingress                    # Control incoming traffic
    - Egress                     # Control outgoing traffic
  ingress:                       # Rules for incoming traffic
    - from:
        - namespaceSelector:     # Allow from specific namespace
            matchLabels:
              name: allowed-ns
      ports:
        - protocol: TCP
          port: 8080
  egress:                        # Rules for outgoing traffic
    - to:
        - namespaceSelector:
            matchLabels:
              name: database-ns
      ports:
        - protocol: TCP
          port: 5432
```

## Understanding Selectors

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         SELECTOR TYPES                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  1. podSelector - Select pods by labels                                  │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ podSelector:                                                     │    │
│  │   matchLabels:                                                   │    │
│  │     app: nginx          ──► Only pods with label app=nginx      │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  2. namespaceSelector - Select namespaces by labels                      │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ namespaceSelector:                                               │    │
│  │   matchLabels:                                                   │    │
│  │     kubernetes.io/metadata.name: auth  ──► From 'auth' namespace│    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  3. ipBlock - Select by IP range                                         │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ ipBlock:                                                         │    │
│  │   cidr: 10.0.0.0/8       ──► From this IP range                 │    │
│  │   except:                                                        │    │
│  │     - 10.0.1.0/24        ──► Except this subnet                 │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## AI Security Platform Network Architecture

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    AI SECURITY PLATFORM - NETWORK FLOW                           │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  EXTERNAL (Browser)                                                              │
│  ┌────────────────┐                                                             │
│  │ User Request   │                                                             │
│  │ *.localhost:80 │                                                             │
│  └───────┬────────┘                                                             │
│          │                                                                       │
│          ▼                                                                       │
│  ┌───────────────────────────────────────────────────────────────────────────┐  │
│  │ NAMESPACE: traefik                                                         │  │
│  │ ┌─────────────────────────────────────────────────────────────────────┐   │  │
│  │ │ TRAEFIK (Ingress Controller)                                         │   │  │
│  │ │ Ports: 80, 443                                                       │   │  │
│  │ │                                                                       │   │  │
│  │ │ Routes:                                                               │   │  │
│  │ │   auth.ai-platform.localhost ──────► auth namespace                  │   │  │
│  │ │   chat.ai-platform.localhost ──────► ai-apps namespace (future)      │   │  │
│  │ └─────────────────────────────────────────────────────────────────────┘   │  │
│  │                                                                            │  │
│  │ NetworkPolicy: traefik-policy                                              │  │
│  │   Ingress: ANY ──► :8000, :8443                                           │  │
│  │   Egress:  ──► auth:8080, ai-apps:8080, DNS                               │  │
│  └────────────────────────────────────────────────────────────────────────────┘  │
│          │                                                                       │
│          │ :8080                                                                │
│          ▼                                                                       │
│  ┌───────────────────────────────────────────────────────────────────────────┐  │
│  │ NAMESPACE: auth                                                            │  │
│  │ ┌─────────────────────────────────────────────────────────────────────┐   │  │
│  │ │ KEYCLOAK (IAM)                                                       │   │  │
│  │ │ Port: 8080                                                           │   │  │
│  │ │                                                                       │   │  │
│  │ │ Functions:                                                            │   │  │
│  │ │   - User authentication                                               │   │  │
│  │ │   - OIDC token issuance                                               │   │  │
│  │ │   - Role management                                                   │   │  │
│  │ └─────────────────────────────────────────────────────────────────────┘   │  │
│  │                                                                            │  │
│  │ NetworkPolicy: keycloak-policy                                             │  │
│  │   Ingress: traefik ──► :8080                                              │  │
│  │   Egress:  ──► storage:5432, DNS                                          │  │
│  └────────────────────────────────────────────────────────────────────────────┘  │
│          │                                                                       │
│          │ :5432                                                                │
│          ▼                                                                       │
│  ┌───────────────────────────────────────────────────────────────────────────┐  │
│  │ NAMESPACE: storage                                                         │  │
│  │ ┌─────────────────────────────────────────────────────────────────────┐   │  │
│  │ │ POSTGRESQL (Database)                                                │   │  │
│  │ │ Port: 5432                                                           │   │  │
│  │ │                                                                       │   │  │
│  │ │ Databases: keycloak, mlflow, openwebui                               │   │  │
│  │ └─────────────────────────────────────────────────────────────────────┘   │  │
│  │                                                                            │  │
│  │ NetworkPolicy: postgresql-policy                                           │  │
│  │   Ingress: auth:5432, ai-apps:5432, cnpg-system:5432                      │  │
│  │   Egress:  ──► DNS, self (replication)                                    │  │
│  └────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                  │
│  ┌───────────────────────────────────────────────────────────────────────────┐  │
│  │ NAMESPACE: cnpg-system                                                     │  │
│  │ ┌─────────────────────────────────────────────────────────────────────┐   │  │
│  │ │ CNPG OPERATOR                                                        │   │  │
│  │ │                                                                       │   │  │
│  │ │ Manages PostgreSQL lifecycle                                          │   │  │
│  │ └─────────────────────────────────────────────────────────────────────┘   │  │
│  │                                                                            │  │
│  │ (Uses ArgoCD's existing policies)                                          │  │
│  └────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Our NetworkPolicies Explained

### 1. storage-netpol.yaml (PostgreSQL)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     POSTGRESQL NETWORK POLICY                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ALLOWED INGRESS (who can connect to PostgreSQL)                        │
│                                                                          │
│    ┌──────────────┐                                                     │
│    │ auth         │                                                     │
│    │ namespace    │─────────────────┐                                   │
│    │ (Keycloak)   │                 │                                   │
│    └──────────────┘                 │                                   │
│                                     ▼                                    │
│    ┌──────────────┐           ┌──────────────┐                         │
│    │ ai-apps      │           │  PostgreSQL  │                         │
│    │ namespace    │──────────►│    :5432     │                         │
│    │ (Open WebUI) │           │              │                         │
│    └──────────────┘           └──────────────┘                         │
│                                     ▲                                    │
│    ┌──────────────┐                 │                                   │
│    │ cnpg-system  │─────────────────┘                                   │
│    │ namespace    │                                                     │
│    │ (Operator)   │                                                     │
│    └──────────────┘                                                     │
│                                                                          │
│  ALLOWED EGRESS (where PostgreSQL can connect)                          │
│                                                                          │
│    ┌──────────────┐           ┌──────────────┐                         │
│    │  PostgreSQL  │──────────►│ kube-system  │  DNS (:53)              │
│    │              │           │   CoreDNS    │                         │
│    │              │──────────►│  PostgreSQL  │  Replication (:5432)    │
│    └──────────────┘           │    (self)    │                         │
│                               └──────────────┘                         │
│                                                                          │
│  ❌ BLOCKED                                                              │
│    - Internet access                                                    │
│    - Other namespaces (traefik, argocd, etc.)                          │
│    - Unknown pods in allowed namespaces                                 │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**File:** `storage-netpol.yaml`
```yaml
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgresql-policy
  namespace: storage
spec:
  podSelector:
    matchLabels:
      cnpg.io/cluster: postgresql-cluster   # Applies to CNPG pods
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # From Keycloak
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: auth
      ports:
        - protocol: TCP
          port: 5432
    # From future ai-apps (Open WebUI, MLflow)
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ai-apps
      ports:
        - protocol: TCP
          port: 5432
    # From CNPG operator
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: cnpg-system
      ports:
        - protocol: TCP
          port: 5432
        - protocol: TCP
          port: 8000   # Metrics
  egress:
    # DNS resolution
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
    # Replication between PostgreSQL pods
    - to:
        - podSelector:
            matchLabels:
              cnpg.io/cluster: postgresql-cluster
      ports:
        - protocol: TCP
          port: 5432
```

---

### 2. auth-netpol.yaml (Keycloak)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       KEYCLOAK NETWORK POLICY                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ALLOWED INGRESS                                                         │
│                                                                          │
│    ┌──────────────┐           ┌──────────────┐                         │
│    │   traefik    │           │   Keycloak   │                         │
│    │  namespace   │──────────►│    :8080     │                         │
│    │              │           │    :8443     │                         │
│    └──────────────┘           └──────────────┘                         │
│                                                                          │
│  ALLOWED EGRESS                                                          │
│                                                                          │
│    ┌──────────────┐           ┌──────────────┐                         │
│    │   Keycloak   │──────────►│   storage    │  PostgreSQL (:5432)     │
│    │              │           │              │                         │
│    │              │──────────►│ kube-system  │  DNS (:53)              │
│    └──────────────┘           └──────────────┘                         │
│                                                                          │
│  ❌ BLOCKED                                                              │
│    - Direct access (bypass Traefik)                                     │
│    - Internet access (no external IdP for now)                          │
│    - Other namespaces                                                   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 3. traefik-netpol.yaml (Ingress Controller)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       TRAEFIK NETWORK POLICY                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ALLOWED INGRESS                                                         │
│                                                                          │
│    ┌──────────────┐           ┌──────────────┐                         │
│    │   ANYWHERE   │           │   Traefik    │                         │
│    │   (0.0.0.0)  │──────────►│    :8000     │  HTTP                   │
│    │              │──────────►│    :8443     │  HTTPS                  │
│    └──────────────┘           └──────────────┘                         │
│                                                                          │
│  ALLOWED EGRESS                                                          │
│                                                                          │
│    ┌──────────────┐           ┌──────────────┐                         │
│    │   Traefik    │──────────►│     auth     │  Keycloak (:8080)       │
│    │              │──────────►│   ai-apps    │  Open WebUI (:8080)     │
│    │              │──────────►│ kube-system  │  DNS (:53)              │
│    └──────────────┘           └──────────────┘                         │
│                                                                          │
│  ❌ BLOCKED                                                              │
│    - Direct database access                                             │
│    - Internal services (ArgoCD, CNPG operator)                          │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Default Deny Pattern

When you create a NetworkPolicy with `podSelector: {}` (empty), it acts as a default deny:

```yaml
# Default deny all ingress
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: my-namespace
spec:
  podSelector: {}        # Applies to ALL pods in namespace
  policyTypes:
    - Ingress            # No ingress rules = deny all ingress
```

**Our approach:** We define explicit allow rules per pod type, which implicitly denies everything else.

---

# Part 2: Pod Security Standards (PSS)

## What is PSS?

Pod Security Standards define three security levels that restrict what pods can do:

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    POD SECURITY STANDARDS LEVELS                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  PRIVILEGED ──────────────────────────────────────────────────────────  │
│  │                                                                       │
│  │  • No restrictions                                                    │
│  │  • Full host access                                                   │
│  │  • Root user allowed                                                  │
│  │  • All capabilities allowed                                           │
│  │                                                                       │
│  │  Use case: System components (kube-system)                           │
│  │                                                                       │
│  ▼                                                                       │
│  BASELINE ────────────────────────────────────────────────────────────  │
│  │                                                                       │
│  │  • Prevents known privilege escalations                              │
│  │  • No hostNetwork, hostPID, hostIPC                                  │
│  │  • No privileged containers                                          │
│  │  • Limited volume types                                               │
│  │  • Some capabilities allowed (NET_BIND_SERVICE)                      │
│  │                                                                       │
│  │  Use case: Ingress controllers, load balancers                       │
│  │                                                                       │
│  ▼                                                                       │
│  RESTRICTED ──────────────────────────────────────────────────────────  │
│                                                                          │
│    • Maximum security                                                    │
│    • Must run as non-root                                               │
│    • No capability additions                                            │
│    • Read-only root filesystem (recommended)                            │
│    • Seccomp profile required                                           │
│                                                                          │
│    Use case: Most applications                                          │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

## PSS Modes

Each level can be applied in three modes:

| Mode | Behavior |
|------|----------|
| **enforce** | Blocks pods that violate the policy |
| **audit** | Logs violations but allows pods |
| **warn** | Shows warnings but allows pods |

**Best practice:** Use all three to catch issues early:
```yaml
labels:
  pod-security.kubernetes.io/enforce: restricted
  pod-security.kubernetes.io/audit: restricted      # Logs violations
  pod-security.kubernetes.io/warn: restricted       # Shows warnings
```

## PSS Comparison Table

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    WHAT EACH LEVEL RESTRICTS                             │
├──────────────────────┬─────────────┬─────────────┬─────────────────────┤
│ Feature              │ Privileged  │ Baseline    │ Restricted          │
├──────────────────────┼─────────────┼─────────────┼─────────────────────┤
│ hostNetwork          │ ✅ Allowed  │ ❌ Blocked  │ ❌ Blocked          │
│ hostPID              │ ✅ Allowed  │ ❌ Blocked  │ ❌ Blocked          │
│ hostIPC              │ ✅ Allowed  │ ❌ Blocked  │ ❌ Blocked          │
│ privileged           │ ✅ Allowed  │ ❌ Blocked  │ ❌ Blocked          │
│ Capabilities (add)   │ ✅ Allowed  │ ⚠️ Limited  │ ❌ Blocked          │
│ HostPath volumes     │ ✅ Allowed  │ ❌ Blocked  │ ❌ Blocked          │
│ runAsRoot            │ ✅ Allowed  │ ✅ Allowed  │ ❌ Blocked          │
│ runAsNonRoot         │ Not req.    │ Not req.    │ ✅ Required         │
│ Seccomp              │ Not req.    │ Not req.    │ ✅ Required         │
│ AppArmor             │ Not req.    │ Not req.    │ ✅ Required         │
└──────────────────────┴─────────────┴─────────────┴─────────────────────┘
```

---

## Our PSS Configuration

```
┌─────────────────────────────────────────────────────────────────────────┐
│              AI SECURITY PLATFORM - PSS ASSIGNMENT                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────────┐                                                    │
│  │   kube-system   │  PRIVILEGED                                        │
│  │                 │  System components need full access                │
│  │  - CoreDNS      │                                                    │
│  │  - kube-proxy   │                                                    │
│  └─────────────────┘                                                    │
│                                                                          │
│  ┌─────────────────┐                                                    │
│  │    traefik      │  BASELINE                                          │
│  │                 │  Needs NET_BIND_SERVICE capability                 │
│  │  - Traefik pod  │  for ports 80/443                                  │
│  │                 │                                                    │
│  └─────────────────┘                                                    │
│                                                                          │
│  ┌─────────────────┐                                                    │
│  │    storage      │  RESTRICTED                                        │
│  │                 │  PostgreSQL runs as non-root                       │
│  │  - PostgreSQL   │  No special capabilities needed                    │
│  │                 │                                                    │
│  └─────────────────┘                                                    │
│                                                                          │
│  ┌─────────────────┐                                                    │
│  │      auth       │  RESTRICTED                                        │
│  │                 │  Keycloak runs as non-root (UID 1000)             │
│  │  - Keycloak     │  No special capabilities needed                    │
│  │                 │                                                    │
│  └─────────────────┘                                                    │
│                                                                          │
│  ┌─────────────────┐                                                    │
│  │   cnpg-system   │  RESTRICTED                                        │
│  │                 │  Operator is well-designed                         │
│  │  - CNPG Op      │  Runs as non-root                                  │
│  │                 │                                                    │
│  └─────────────────┘                                                    │
│                                                                          │
│  ┌─────────────────┐                                                    │
│  │     argocd      │  RESTRICTED (by default)                           │
│  │                 │  ArgoCD components run as non-root                 │
│  │  - All comps    │                                                    │
│  │                 │                                                    │
│  └─────────────────┘                                                    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Applying PSS via GitOps

We manage namespace PSS labels through ArgoCD:

**File:** `namespaces.yaml`
```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: storage
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: v1
kind: Namespace
metadata:
  name: auth
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: v1
kind: Namespace
metadata:
  name: traefik
  labels:
    pod-security.kubernetes.io/enforce: baseline    # Needs capabilities
    pod-security.kubernetes.io/audit: baseline
    pod-security.kubernetes.io/warn: baseline
---
apiVersion: v1
kind: Namespace
metadata:
  name: cnpg-system
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
```

---

# Part 3: Implementation Details

## Directory Structure

```
argocd/applications/security/security-baseline/
├── application.yaml              # ArgoCD Application definition
└── manifests/
    ├── storage-netpol.yaml       # PostgreSQL NetworkPolicy
    ├── auth-netpol.yaml          # Keycloak NetworkPolicy
    ├── traefik-netpol.yaml       # Traefik NetworkPolicy
    └── namespaces.yaml           # PSS labels on namespaces
```

## How ArgoCD Deploys This

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    GITOPS FLOW FOR SECURITY                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  1. Git Repository                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ argocd/applications/security/security-baseline/                  │    │
│  │   ├── application.yaml                                           │    │
│  │   └── manifests/*.yaml                                           │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                           │
│                              ▼                                           │
│  2. root-app (App-of-Apps)                                              │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Detects: security/security-baseline/application.yaml             │    │
│  │ Creates: Application "security-baseline" in ArgoCD               │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                           │
│                              ▼                                           │
│  3. security-baseline Application                                        │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Watches: manifests/ directory                                    │    │
│  │ Deploys: All YAML files to cluster                               │    │
│  │   - NetworkPolicies to respective namespaces                     │    │
│  │   - Namespace labels (PSS)                                       │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                           │
│                              ▼                                           │
│  4. Kubernetes Cluster                                                   │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ NetworkPolicies enforced by CNI (k3s uses Flannel/kube-router)  │    │
│  │ PSS enforced by Pod Security Admission controller               │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

# Part 4: Verification & Testing

## Verify NetworkPolicies

```bash
# List all NetworkPolicies
kubectl get networkpolicies -A

# Describe a specific policy
kubectl describe networkpolicy postgresql-policy -n storage

# Test connectivity (should work)
kubectl exec -it postgresql-cluster-1 -n storage -- nc -zv localhost 5432

# Test from allowed namespace (should work)
kubectl run test-pod --rm -it --image=busybox -n auth -- nc -zv postgresql-cluster-rw.storage.svc 5432

# Test from disallowed namespace (should fail/timeout)
kubectl run test-pod --rm -it --image=busybox -n default -- nc -zv postgresql-cluster-rw.storage.svc 5432
```

## Verify PSS Labels

```bash
# Check namespace labels
kubectl get ns --show-labels | grep pod-security

# Expected output:
# storage    pod-security.kubernetes.io/enforce=restricted
# auth       pod-security.kubernetes.io/enforce=restricted
# traefik    pod-security.kubernetes.io/enforce=baseline
# cnpg-system pod-security.kubernetes.io/enforce=restricted
```

## Test PSS Enforcement

```bash
# Try to create a privileged pod in restricted namespace (should fail)
kubectl run test-privileged --image=nginx -n storage \
  --overrides='{"spec":{"containers":[{"name":"test","image":"nginx","securityContext":{"privileged":true}}]}}'

# Expected: Error - violates PodSecurity "restricted:latest"
```

---

# Part 5: Troubleshooting

## NetworkPolicy Not Working

```bash
# 1. Check if CNI supports NetworkPolicies
kubectl get pods -n kube-system | grep -E "flannel|calico|cilium"

# 2. Verify policy is applied
kubectl get networkpolicy -n <namespace>

# 3. Check pod labels match selector
kubectl get pods -n storage --show-labels

# 4. Test with a debug pod
kubectl run debug --rm -it --image=nicolaka/netshoot -n auth -- bash
# Inside: curl postgresql-cluster-rw.storage.svc:5432
```

## PSS Blocking Pods

```bash
# 1. Check events for violations
kubectl get events -n <namespace> | grep -i security

# 2. Check pod security context
kubectl get pod <pod-name> -n <namespace> -o yaml | grep -A 20 securityContext

# 3. Temporarily switch to audit mode to see violations
kubectl label ns <namespace> pod-security.kubernetes.io/enforce=privileged --overwrite
kubectl label ns <namespace> pod-security.kubernetes.io/audit=restricted --overwrite
```

---

# Summary

## What We Implemented

| Component | Count | Purpose |
|-----------|-------|---------|
| **NetworkPolicies** | 3 | Segment network between namespaces |
| **PSS Labels** | 4 namespaces | Restrict pod capabilities |

## Security Posture Before/After

```
BEFORE Phase 4:
┌─────────────────────────────────────────────────────────────────────────┐
│  ❌ All pods can communicate with all pods                              │
│  ❌ Pods can run as root                                                │
│  ❌ Pods can mount host filesystem                                      │
│  ❌ No network segmentation                                             │
└─────────────────────────────────────────────────────────────────────────┘

AFTER Phase 4:
┌─────────────────────────────────────────────────────────────────────────┐
│  ✅ Only allowed traffic between namespaces                             │
│  ✅ Pods must run as non-root (restricted namespaces)                  │
│  ✅ No privileged containers allowed                                    │
│  ✅ Clear network segmentation enforced                                 │
└─────────────────────────────────────────────────────────────────────────┘
```

## Files Reference

| File | Purpose |
|------|---------|
| `application.yaml` | Tells ArgoCD to manage security-baseline |
| `storage-netpol.yaml` | PostgreSQL network rules |
| `auth-netpol.yaml` | Keycloak network rules |
| `traefik-netpol.yaml` | Traefik network rules |
| `namespaces.yaml` | PSS labels for all namespaces |

---

---

# Part 6: TLS with cert-manager

## What is cert-manager?

cert-manager automates certificate management in Kubernetes. It can issue certificates from various sources:

| Issuer Type | Use Case |
|-------------|----------|
| Self-signed | Development, home lab |
| Let's Encrypt | Production with public domain |
| Vault | Enterprise PKI |
| Internal CA | Corporate environments |

## Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    CERT-MANAGER FLOW                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  1. Ingress with annotation                                              │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ metadata:                                                        │    │
│  │   annotations:                                                   │    │
│  │     cert-manager.io/cluster-issuer: ai-platform-ca-issuer       │    │
│  │ spec:                                                            │    │
│  │   tls:                                                           │    │
│  │     - hosts: [argocd.ai-platform.localhost]                     │    │
│  │       secretName: argocd-tls                                    │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                           │
│                              ▼                                           │
│  2. cert-manager detects annotation                                      │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ cert-manager watches Ingress resources                          │    │
│  │ Sees: cert-manager.io/cluster-issuer annotation                 │    │
│  │ Creates: Certificate resource automatically                      │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                           │
│                              ▼                                           │
│  3. Certificate issued                                                   │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ ClusterIssuer signs the certificate                             │    │
│  │ Secret created: argocd-tls                                      │    │
│  │ Contains: tls.crt, tls.key                                      │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                              │                                           │
│                              ▼                                           │
│  4. Traefik uses the certificate                                        │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ Traefik reads Secret from Ingress tls config                    │    │
│  │ Serves HTTPS with the certificate                               │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

## Our Setup: Self-Signed CA

For the home lab, we use a self-signed CA chain:

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    CERTIFICATE CHAIN                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────────────────┐                                               │
│  │  selfsigned-issuer   │  ClusterIssuer (bootstrap)                    │
│  │                      │  Creates the root CA                          │
│  └──────────┬───────────┘                                               │
│             │                                                            │
│             ▼                                                            │
│  ┌──────────────────────┐                                               │
│  │   ai-platform-ca     │  Certificate (CA)                             │
│  │                      │  Valid 10 years                               │
│  │   Secret:            │  Stored in cert-manager namespace             │
│  │   ai-platform-ca-secret                                              │
│  └──────────┬───────────┘                                               │
│             │                                                            │
│             ▼                                                            │
│  ┌──────────────────────┐                                               │
│  │ ai-platform-ca-issuer│  ClusterIssuer (production)                   │
│  │                      │  Signs all app certificates                   │
│  └──────────┬───────────┘                                               │
│             │                                                            │
│             ├────────────────┬────────────────┐                         │
│             ▼                ▼                ▼                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │
│  │  argocd-tls  │  │ keycloak-tls │  │  future...   │                  │
│  │              │  │              │  │              │                  │
│  │  namespace:  │  │  namespace:  │  │              │                  │
│  │    argocd    │  │    auth      │  │              │                  │
│  └──────────────┘  └──────────────┘  └──────────────┘                  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

## Implementation Files

### cert-manager Application (Helm chart)

**File:** `argocd/applications/infrastructure/cert-manager/application.yaml`
```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://charts.jetstack.io
    chart: cert-manager
    targetRevision: v1.17.2
    helm:
      parameters:
        - name: installCRDs
          value: "true"
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
```

### ClusterIssuers Configuration

**File:** `argocd/applications/infrastructure/cert-manager-config/manifests/cluster-issuer.yaml`
```yaml
# Self-signed ClusterIssuer for home lab TLS
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
spec:
  selfSigned: {}
---
# CA Certificate (signs other certificates)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ai-platform-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: ai-platform-ca
  secretName: ai-platform-ca-secret
  duration: 87600h  # 10 years
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
---
# ClusterIssuer using our CA
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: ai-platform-ca-issuer
spec:
  ca:
    secretName: ai-platform-ca-secret
```

## Adding TLS to Ingress Resources

To enable TLS on any Ingress, add:

```yaml
metadata:
  annotations:
    cert-manager.io/cluster-issuer: ai-platform-ca-issuer
spec:
  tls:
    - hosts:
        - <your-hostname>
      secretName: <name>-tls
```

### Example: ArgoCD Ingress with TLS

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-ingress
  namespace: argocd
  annotations:
    cert-manager.io/cluster-issuer: ai-platform-ca-issuer
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - argocd.ai-platform.localhost
      secretName: argocd-tls
  rules:
    - host: argocd.ai-platform.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80
```

## Verification Commands

```bash
# Check cert-manager pods
kubectl get pods -n cert-manager

# List ClusterIssuers
kubectl get clusterissuers

# List all certificates
kubectl get certificates -A

# Check certificate details
kubectl describe certificate argocd-tls -n argocd

# Check certificate secret
kubectl get secret argocd-tls -n argocd -o yaml
```

## Browser Warning

Since we use self-signed certificates, browsers will show a warning. This is expected for home lab. Click "Advanced" → "Accept Risk" to proceed.

---

# Part 7: Exposing ArgoCD via Traefik

## Why?

Before: `kubectl port-forward svc/argocd-server -n argocd 9090:443`
After: `https://argocd.ai-platform.localhost`

No more manual port-forward!

## Configuration Steps

### 1. Enable HTTP mode in ArgoCD

ArgoCD defaults to HTTPS. For Traefik to handle TLS termination:

```bash
kubectl patch configmap argocd-cmd-params-cm -n argocd --type merge -p '{"data":{"server.insecure":"true"}}'
kubectl rollout restart deployment argocd-server -n argocd
```

### 2. Create Ingress with TLS

See ArgoCD Ingress example above in Part 6.

### 3. Update Windows hosts file

Add to `C:\Windows\System32\drivers\etc\hosts`:
```
127.0.0.1 argocd.ai-platform.localhost
```

---

# Summary

## Final Security Configuration

| Namespace | PSS Level | Reason |
|-----------|-----------|--------|
| storage | restricted | PostgreSQL compliant |
| auth | **baseline** | Keycloak needs some capabilities |
| traefik | baseline | Ingress controller needs NET_BIND |
| cnpg-system | restricted | Operator compliant |

> **Note:** We initially set auth to `restricted` but Keycloak's default securityContext doesn't fully comply. Using `baseline` is acceptable for IAM components.

## TLS Enabled Services

| Service | URL | Certificate |
|---------|-----|-------------|
| ArgoCD | https://argocd.ai-platform.localhost | argocd-tls |
| Keycloak | https://auth.ai-platform.localhost | keycloak-tls |

## Components Deployed in Phase 4

| Component | Namespace | Purpose |
|-----------|-----------|---------|
| NetworkPolicies (3) | storage, auth, traefik | Network segmentation |
| PSS Labels | 4 namespaces | Pod security enforcement |
| cert-manager | cert-manager | Certificate automation |
| ClusterIssuers (2) | cluster-wide | Certificate signing |
| Ingress (ArgoCD) | argocd | External access |

## Next Steps

- [ ] Phase 5: Ollama + Mistral 7B (AI Inference)
- [ ] Add NetworkPolicies for ai-apps, ai-inference namespaces
- [ ] Integrate ArgoCD with Keycloak OIDC
